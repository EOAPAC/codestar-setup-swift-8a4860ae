Revise the "What this award does not do" section

Replace the two items that miss the mark with new ones that are factually accurate and still useful for setting expectations.

- In `src/routes/index.tsx`, update the `doesNotDo` array:
  - Remove "No ceremony" (physical awards exist, so this is misleading).
  - Remove "No renewal" (does not hit the mark).
  - Add "No categories" — there are no sector awards, size brackets, or separate categories; every entry is read against the same three criteria.
  - Add "No membership" — the award is not a club or network; it is a single, dated recognition with no recurring commitment.

- Keep the existing dark inverted section styling and four-column hairline grid layout (`lg:grid-cols-4`), since the list will still contain four items.

- Verify the new copy matches the existing tone: short title, plain explanation, no promises or marketing language.
